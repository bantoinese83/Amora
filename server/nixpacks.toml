[phases.setup]
nixPkgs = ["nodejs_20", "npm-9_x"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build", "ls -la dist/ || echo 'Build failed - dist folder not found'"]

[start]
cmd = "node dist/index.js"

