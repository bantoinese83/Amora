[phases.setup]
nixPkgs = ["nodejs_20", "npm-9_x"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build", "ls -la dist/", "test -f dist/index.js || (echo 'ERROR: dist/index.js not found!' && exit 1)"]

[start]
cmd = "pwd && ls -la && ls -la dist/ && node dist/index.js"

